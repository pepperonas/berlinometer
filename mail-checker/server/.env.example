# Example environment variables for mail-checker server
# Copy this file to .env and fill in your values

# Server configuration
PORT=5076

# Optional: Default mail server settings
DEFAULT_IMAP_PORT=993
DEFAULT_TLS=true